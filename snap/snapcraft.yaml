name: firewall-gael
version: "0.1"
base: core20
summary: Firewall script launcher
description: Firewall script launcher

grade: stable
confinement: strict
license: GPL-3.0

architectures:
  - build-on: arm64
  - build-on: armhf
  - build-on: amd64

apps:
  srv:
    command: bin/srv
    daemon: oneshot
    plugs: [ firewall-control ]
  clear:
    command: bin/clear
    plugs: [ firewall-control ]

parts:
  wrapper:
    plugin: dump
    source: snap/local
